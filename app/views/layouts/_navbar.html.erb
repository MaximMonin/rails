<nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm" id="appBar">
    <div class="container">
        <!-- Right Side Of Navbar -->
        <ul class="navbar-nav ml-auto">
            <!-- Authentication Links -->
            <% if !current_user %>
                    <li class="nav-item">
                        <a class="navbar-brand" href="/users/sign_up">Sign Up</a>
                    </li>
            <% else %>
                <li class="nav-item dropdown">
                    <% if current_user.avatar %>
                       <img class="centered-and-cropped" width="30" height="30" style="border-radius:50%" src="<%= current_user.avatar %>"> 
                    <% end %>
                    <a id="navbarDropdown" class="navbar-brand dropdown-toggle" dusk="user-button" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= current_user.username %> <span class="caret"></span>
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="profile">
                            Profile
                        </a>
                        <a class="dropdown-item" href="fileupload">
                            Files
                        </a>
                        <a class="dropdown-item" href="chat">
                            Chat
                        </a>
                        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
                    </div>
                </li>
            <% end %>
            <li class="nav-item dropdown">
                <a id="navbarDropdown" class="navbar-brand dropdown-toggle" dusk="lang-button" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Language <span class="caret"></span>
                </a>

                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="locale/en">
                        EN
                    </a>
                    <a class="dropdown-item" href="locale/ru">
                        RU
                    </a>
                    <a class="dropdown-item" href="locale/uk">
                        UK
                    </a>
                </div>
            </li>
        </ul>
    </div>
</nav>
